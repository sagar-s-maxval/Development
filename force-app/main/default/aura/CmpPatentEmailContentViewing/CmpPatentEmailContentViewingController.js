({	
                doInit : function(component, event, helper) {
                   // var rec = component.get("v.recordId");
                   //var rec='a1Qf0000000do38EAA';
                   // alert(rec);
                    helper.getPatentEmailRecord(component, event, helper); // Calling Helper method
                }
})