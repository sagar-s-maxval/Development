({
    /*handleInputChange : function(component, event, helper) {
        var richTextCmp = component.find("richText");
        var inputValue = richTextCmp.get("v.value");
        var maxCharacters = component.get("v.maxCharacters");
        
        if (inputValue.length > maxCharacters) {
            richTextCmp.set("v.value", inputValue.substring(0, maxCharacters));
            component.set("v.remainingCharacters", 0);
            component.set("v.errorMessage", "You have reached the maximum character limit.");
        } else {
            var remainingChars = maxCharacters - inputValue.length;
            component.set("v.remainingCharacters", remainingChars);
            component.set("v.errorMessage", "");
        }
    }*/
})